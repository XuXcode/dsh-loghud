# Node.js demo

These dependency-free scripts reproduce errors recognized by LogHUD v0.2.1:

```sh
npm run type-error
npm run module-missing
npm run port-in-use
```

Use an ordinary Harness shell to update LogHUD after completion, or `loghud_run` for incremental monitoring.
